name: Daily Mood Update

on:
  schedule:
    - cron: "0 4 * * *"

jobs:
  update_message:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run update script
        run: chmod +x mood.sh && ./mood.sh